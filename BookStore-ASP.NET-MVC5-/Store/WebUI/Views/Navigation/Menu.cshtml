@model IEnumerable<string>


@Html.ActionLink("Home","List","Books")
@foreach(var link in Model)
{

    @Html.RouteLink(link, new
{
    Controller = "Books",
    action = "List",
    genre = link,
    page = 1
})

}