@model Domain.Entities.Book

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
   <div class="panel">
    <div class="panel-heading">
        <h3>Edit book:@Model.Name</h3>

    </div>
@using (Html.BeginForm("Delete", "Admin", FormMethod.Post))
            {
    <div class="panel-body">
        @Html.HiddenFor(m=>m.BookId)
        @foreach (var property in ViewData.ModelMetadata.Properties)
        {

            if (property.PropertyName != "BookId")
            {<div class="form-group">
            <label> @Html.Display(property.PropertyName)</label>
        </div>
            }
        }

</div>
        <div class="panel-footer">

         
                    <input type="submit" value="Delete" class="btn btn-default" />
                    @Html.ActionLink("Cancel", "Index", null, new { @class = "btn btn-dander" });
           
        </div>
}
        </div>

  